<div class="d-none group-form"
     data-index-collection-target= "form">
  <%= simple_form_for [current_user, Collection.new] do |f| %>
    <%= f.input :name %>
    <%= f.input :description %>
    <%= f.submit :create, class: "btn btn-success text-white valid" %>
  <% end %>
</div>
